# Serverless functions demo brief

- Stable ID, leaf category, English/Korean/Japanese titles: `serverless-functions`; `hosting-models`; Serverless functions / 서버리스 함수 / サーバーレス関数.
- Definition (one sentence): Serverless functions run managed handlers in response to events or requests. The platform manages servers; application correctness still belongs to the developer.
- Closest concept and concrete difference: compare static-hosting, always-on-server; Two independent invocation boxes sharing one reader/article key and external store.
- Distinguishing visual features (structure, material, typography): Two independent invocation boxes sharing one reader/article key and external store. Restrained solid surfaces, system text and tabular counts keep mechanism labels legible; colors are editorial, not product definitions.
- Shared comparison category: [hosting-models](../catalog-writing/groups/hosting-models.md).
- Article-specific scenario, dataset, labels, actions, and initial state: Public article available; reader/Festival Schedule absent; generation one; call count zero.
- Visual variable changed; concept-specific extra controls and their justification: Two independent invocation boxes sharing one reader/article key and external store. Failure and boundary controls expose the documented application assumptions.
- Fictional scenario and why it demonstrates the definition: A festival schedule gets occasional save requests, and a constantly running app would sit idle most of the day. Read article invokes a handler.
- Representative action and observable result: Read article invokes a handler. Save Festival Schedule invokes another handler and writes an external store.
- Initial state: Public article available; reader/Festival Schedule absent; generation one; call count zero.
- Changed state, repeated action, empty input/no results: Read follows the labelled delivery path. Save writes once by reader/article key. Failure occurs before write. Restart clears execution counters, increments generation and preserves illustrated external records. No free-form input is accepted; no samples is an explicit canary fixture. All values are authored examples, not measurements.
- Reset and reload behavior: Restore the entire initial page-memory model, including the illustrative store. Hosting restart preserves its store; blue-green Previous and switch-back preserve writes. Only Reset/reload clears those records.
- Mobile order and width thresholds: Controls, read and write lanes, execution boundary, external store, response and explanatory note. Diagram grids stack below 560px of demo content width. At 320px viewport, labels wrap rather than shrink. The scene SVG uses a viewBox and a full-size textual legend.
- Keyboard order, focus, accessible names, live feedback: Read article → Save Festival Schedule → Restart handler → Reset → Fail next save. The failure checkbox is consumed by the next save. Native buttons/checkboxes, dashed visible focus, mounted controls, polite atomic status, no timer or drag-only control. Reset retains focus. Disabled step buttons mark boundaries.
- Light/dark surrounding themes, opaque fallback, no shadows, reduced motion: Solid authored light surfaces in both site themes; no transparent/shadow-dependent content or animation. Numbers, version names, outlines and text survive forced colors.
- JavaScript-disabled initial screen and explanation: Server-rendered initial diagram, legend, captions and Markdown remain readable. All interactive buttons and checkboxes start disabled. Wrapper explains the JavaScript requirement.
- Font families, supported characters, fallback and measurement method: System sans-serif with platform CJK fallback; monospace only for file/key tokens. No font-metric claims or extra downloaded fonts.
- Localized visible strings, input constraints, original/translation review: English group argument reviewed before Korean/Japanese translation on 2026-09-22. All interface labels, responses and states are localized; identifiers and engine API type names remain literal. No native-speaker review is claimed.
- Capture selector and initial content: `[data-demo="serverless-functions"]`; reset at 1440px, light theme, all three languages, initial state above.
- Source URLs, inspected date, claims each source supports: https://docs.aws.amazon.com/lambda/latest/dg/welcome.html. Inspected 2026-09-22; definition/mechanism only. See [source refresh](../catalog-writing/platform-sources.md) for exact passages. Choice advice is editorial; fixture numbers and failure rules are authored.
- Comparison summaries: features: Event-triggered managed handlers; advantages: Platform handles server provisioning; limitations: Retries and lifecycle need design; suitable: Event-oriented APIs; combinations: Static frontend plus external storage.
- Verification commands and evidence paths: Follow [design gates](../design-demos.md), `tests/platforms.test.mjs`, `tests/browser/platforms.spec.ts` and registry-wide browser checks. Captures: `public/thumbnails/serverless-functions-{en,ko,ja}.png`, `artifacts/platform-demos/serverless-functions-*.png`. Actual results belong to [platform review](../platform-demos-review.md).
